# Sustainability Kernel v0.1 — Selective Adoption Record

**Status:** non-normative adoption record  
**Target predecessor:** `0885e230872c546a395cab5c1c5319f99ba3d0c2`  
**Historical source branch:** `parallel/sustainability-backlog-v0.1`

## Why this is a selective adoption

The historical side-track accumulated a broad experimental sustainability and continuity workspace. Its purpose was to develop recoverability, non-interference, bounded observation, human-control, archival, and convergence mechanisms while the CHSP main line continued independently.

That historical branch is intentionally **not** merged, rebased, or replayed wholesale.

This adoption carries forward only the compact invariant kernel that survived review after CHSP v1.0 was merged into `main`.

`historical side-track != integration unit`

`selected invariant set = integration candidate`

## Fresh observation frontier

This adoption was prepared only after observing the post-merge main commit:

`0885e230872c546a395cab5c1c5319f99ba3d0c2`

which merged CHSP v1.0 Bounded External Transition Executor & Receipt.

The adoption therefore does not rely on the earlier side-track observation frontier as if main were unchanged.

## Source provenance

Compact source artifacts on the historical side-track:

- `docs/PARALLEL-SUSTAINABILITY-KERNEL-v0.1.md`
  - source blob SHA: `5e3cb469d356b459d5078eec7ff1c9711ca038f3`
- `schemas/sustainability/v0.1/sustainability-kernel.json`
  - source blob SHA: `d4141fb16dbe9902efdecf86dd829c2dbf39de04`

The historical branch remains a provenance source and exploratory archive. Its existence does not create a requirement to integrate its remaining artifacts.

## CHSP v1.0 overlap review

After CHSP v1.0 entered main, the kernel was re-read against that new boundary.

### Partial semantic overlap

K2 — information is not authority  
K3 — freshness before re-entry  
K4 — attributable capability expansion

These are compatible with CHSP v1.0's exact authorization, stale-request/replay protection, fresh provider preflight, post-write re-observation, and bounded collaborator execution.

They are retained only as project-level invariants. They do not replace CHSP schemas, receipts, policies, tests, or executor semantics.

### Orthogonal sustainability scope

K1 — main-line independence  
K5 — continuity is not permission continuity  
K6 — no merge entitlement  
K7 — selective adoption  
K8 — provenance-preserving supersession  
K9 — human-sustainability has no authority effect  
K10 — bounded observation

These address project continuity and development-process safety outside CHSP v1.0's specific executor scope.

## Deliberately not adopted

The following historical side-track material remains outside this candidate:

- experimental recovery and handoff schemas;
- saturation and divergence fixtures;
- local side-track test suite;
- parallel branch isolation tooling;
- observation/fence fixtures tied to the old main frontier;
- historical backlog and audit documents;
- any mechanism that would create a required CI gate or workflow dependency.

Those artifacts may remain useful as provenance or research inputs, but they are not dependencies of this candidate.

## Authority and execution boundary

This adoption record creates no authority effect.

It does not:

- invoke CHSP v1.0 execution;
- validate an execution request;
- authorize collaborator mutation;
- activate KONTUR;
- create a new permission ceiling for any existing agent;
- change canonical tags, releases, or checkpoints;
- create required checks or workflow dependencies.

## Future disposition

If accepted, the two kernel artifacts may live in main as non-normative project-sustainability guidance.

The historical side-track may then remain isolated or later be archived/superseded with its provenance preserved.

No further adoption from that branch is implied by accepting this kernel.
