# Component Manifest v0.1 origin

Issue #631 was opened from repository `main` after commit:

`2707fdcabb06967ef1d0a60cae8d9123dada8ef2`

The committed component examples bind their described source components to that predecessor frontier. The tooling implementation itself is a successor on the issue branch.

```text
Described Component Frontier != Tooling Successor Frontier
Metadata Description != Source Mutation
```
