# Extraction rationale

Component Manifest v0.1 is justified by repetition already visible across Core, IAL, FCL, integration profiles, product pilots and MarketCloser: each independently binds source identity, schemas, runtime entrypoints, predecessor dependencies, conformance commands and non-effects.

The first slice extracts only that repeated engineering metadata. Domain-specific semantics remain with their owners.
