# Status

First slice target: metadata-only reusable tooling.

No existing Core, IAL, FCL, transport, KONTUR, product or application runtime is modified by this directory.

The branch is not merge-ready until both committed example hashes are materialized, the roadmap is updated and the dedicated CI passes.
