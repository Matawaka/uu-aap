# Reconciliation audit evidence

Each pass records evidence sufficient for deterministic classification without mutating GitHub issue state.

Required implementation evidence normally includes the issue reference, merged implementation PR(s), merge SHA(s), canonical implementation path(s), successor usage where available, and an explicit acceptance assessment.

Roadmap relevance is a separate evidence dimension. Historical completion does not imply current relevance, and current irrelevance does not erase historical completion.
