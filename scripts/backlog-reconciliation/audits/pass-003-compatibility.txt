Dual-axis reconciliation extends the audit model without rewriting historical implementation evidence. Legacy single-axis `state` is intentionally retired in favor of explicit `implementation_state` and `roadmap_state`.
