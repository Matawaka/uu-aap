'use strict';const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const a=JSON.parse(fs.readFileSync(path.join(__dirname,'pass-010.json'),'utf8'));assert.equal(a.corrections[0].issue_ref,'#341');assert.equal(a.corrections[0].previous_state,'INSUFFICIENT_EVIDENCE');assert.equal(a.corrections[0].implementation_state,'COMPLETED');assert.equal(a.corrections[0].merged_pr,'#348');assert.equal(a.historical_closures.length,6);assert.deepEqual(a.preserved_open_roles,[{issue_ref:'#422',roadmap_role:'TARGET_SURFACE'},{issue_ref:'#718',roadmap_role:'WAITING_EXTERNAL'}]);assert.equal(a.non_effects.earlier_audit_rewritten,false);console.log('backlog reconciliation pass 010: ok');