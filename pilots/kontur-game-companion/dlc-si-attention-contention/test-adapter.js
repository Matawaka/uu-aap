'use strict';
const assert=require('node:assert/strict');
const {contestedCueReceipt}=require('./adapter.js');
const base={contention_id:'kontur-cue-001',source_conflict_refs:['kontur:focus:1'],claims:[{claim_id:'cue-a',legitimacy_ref:'evidence:a',successor_state_ref:'focus:a',correct_answer:false,instruction:false},{claim_id:'cue-b',legitimacy_ref:'evidence:b',successor_state_ref:'focus:b',correct_answer:false,instruction:false}],safe_work:['observe_without_cue'],evaluated_at:'2026-08-29T19:05:00Z'};
const deferred=contestedCueReceipt({...base,resolution:{mode:'DEFERRED',selected_claim_id:null,justification:'preserve both cues while channel is singular',lease:null,revisit_triggers:['new_evidence']}});
assert.equal(deferred.status,'DEFERRED');assert.equal(deferred.selected_cue_id,null);assert.deepEqual(deferred.preserved_cue_ids,['cue-a','cue-b']);assert.equal(deferred.correct_answer,false);assert.equal(deferred.instruction,false);assert.equal(deferred.normative_winner,false);assert.equal(deferred.action_permit_created,false);
const temporary=contestedCueReceipt({...base,evaluated_at:'2026-08-29T19:05:30Z',resolution:{mode:'TEMPORARY_PRECEDENCE',selected_claim_id:'cue-a',justification:'bounded cue precedence only',lease:{authority_ref:'human-policy:cursor-v1',scope:['cursor:indication-only'],starts_at:'2026-08-29T19:05:00Z',expires_at:'2026-08-29T19:06:00Z',revocation_conditions:['player_decline'],revisit_triggers:['lease_expiry','new_evidence'],successor_state_constraints:['no-solution-disclosure']},revisit_triggers:['lease_expiry','new_evidence']}});
assert.equal(temporary.status,'TEMPORARY_PRECEDENCE');assert.equal(temporary.selected_cue_id,'cue-a');assert.equal(temporary.precedence_effective,true);assert.equal(temporary.correct_answer,false);assert.equal(temporary.selection_erases_legitimacy,false);assert.equal(temporary.external_effect_authority_created,false);
assert.throws(()=>contestedCueReceipt({...base,claims:[{...base.claims[0],correct_answer:true},base.claims[1]],resolution:{mode:'DEFERRED',selected_claim_id:null,justification:'x',lease:null,revisit_triggers:['new_evidence']}}),/cannot assert correct answer/);
console.log('kontur dlc-si attention contention tests: ok');
