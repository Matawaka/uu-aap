# v0.16 bounded evidence status

First complete A/B head: `3918bd05054e722d7110b5084e610bfa7ea7991d`;
run `34232564112`; A job `102082100161`, B job `102082099863`.
Both succeeded on distinct runners. The downloaded archives have 24 evidence
members that match byte-for-byte, including the exact predeclared request oracle.

This is observed construction of 251 plain zero-shot requests, not dispatch.
Each job executes three full builds (same-Task rebuild and fresh adapter included),
753 real construct_requests calls and 753 Instance initializations. All full
contexts, generation settings, document bindings and metadata match; response
containers are empty. The literal description's final newline is preserved and
no target answer is appended. The qualified v0.15 adapter remains unchanged.

Result SHA-256: `fbee1820bd69778462ca8ea74d91f9ea28a0d547a4e7921af05c881538186d5b`.
Requests SHA-256: `05740117ac6a6b139d51882873f99eafa0373c6874eb00c7871ee8772bbc36be`.
The original request contract predates these observations and was not changed.

The initial observer serialization failure is preserved in history/ and
OBSERVATION-HISTORY.md. It happened before request building; it is not a request
compatibility result or environment regression. Corrected gate blob is
`f63454b95b3ae6f748447eba0150bb58375723a6`.

The successor must execute both A/B jobs again and pass the unconditional fresh
evidence validator. Frozen files are not copied into the new runtime results.
Missing evidence or changed member bytes fail closed. Full request/census files
remain supplementary Actions artifacts (30-day retention); result, contract,
member manifest, history and qualification receipt are durable Git evidence.

Original upstream Task remains NONPASS. The established scope does not include
chat templates, model tokenization/context-window fit, actual backend generation,
stopping behavior, response/scoring compatibility or benchmark performance.
No model, scoring/judge, provider, Hub load, credentials, cache use, evaluation
dispatch, main change, release, standards or merge authority.

Next separate gate: model-free response-to-scoring contract audit, distinguishing
synthetic response controls from model-generated answers. No such scoring probe
has been executed in v0.16, and it requires its own predeclared boundary.
