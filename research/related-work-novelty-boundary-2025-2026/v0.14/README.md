# StateBench namespace-safe selected environment v0.14

Tracks #984; additive successor to **blocked**, not qualified, v0.13 (#983).

Initial status: `CANDIDATE_AWAITING_INDEPENDENT_ENVIRONMENT_OBSERVATION`.
Exact predecessor: `497002001f9eac4adafe2db525d5bb07d9205d21`.
Frozen v0.13 subtree: `434414badeea8ffdaa05e827c9212bd10a2dc0ca`.
Last fully qualified predecessor before this stage: v0.12
`98f13086f8e2d75fc6692ff68e21700bdffa37a5`.

## Explicit intervention, not a repaired historical environment

The v0.10 lock and project file remain unchanged. The separate environment profile
adds an explicit installation selection to exact uv 0.12.10:

```sh
uv sync --frozen --no-install-package statebench-lm-eval --project <exact-v0.12-worktree>/research/related-work-novelty-boundary-2025-2026/v0.10/lock-project
```

This does NOT claim installation of all dependencies declared by the old root
project. It intentionally excludes one distribution, while retaining all other
selected locked dependencies. The predeclared Linux/Python 3.12.14 membership is
129 distributions, compared with the old observed 130. A separate runtime
requirements-closure check includes `lm-eval[hf]`; `uv pip check` also must pass.

The unchanged plugin is used only as exact commit/blob-bound external source.
It is not installed into `lm_eval`, repackaged, added to PYTHONPATH, or used to
patch the initializer. No package is installed then uninstalled; no reinstall,
order manipulation or retry-until-GREEN is allowed. The old conflicting ownership
observation is copied byte-for-byte into `history/`, with its original digest.
The v0.13 blocker and all prior receipts remain historical facts.

## Namespace proof and independent runs

Two predeclared Actions jobs each start with their own empty environment and uv
cache. The census resolves every installed RECORD claim, checks every non-bytecode
file under `lm_eval/`, requires exactly one owner (`lm-eval`), verifies sizes and
SHA-256s, rejects missing/unowned files and symlinks, and binds seven critical
Task/import source files to exact upstream blobs. Generated bytecode is explicitly
excluded. This is an `lm_eval/` namespace audit, NOT a complete filesystem audit of
all dependencies, and NOT proof of a hermetic build of every source distribution.

The public `from lm_eval.tasks import TaskManager` is tested before and after the
source probes in separate `python -I` processes. No internal-import bypass is used.
Each runtime is non-root, has a verified separate network namespace, and has no
model/provider credentials. Import probes additionally use a socket and call guard.
Namespace and import observations must be byte-identical before/after.

## Preserve what failed and what passed

Unchanged v0.11 import, v0.12 exact-data processing and v0.13 Task probes are executed
in the NEW selected environment. Their existing frozen result, document and
format-trace bytes must reproduce. Their validators run in exact detached
predecessor worktrees; no old additive guard is weakened to accept v0.14.

A v0.14 environment PASS must coexist with the reproduced v0.13 Task NONPASS:
empty upstream group, unsupported configuration keys, and a list without features.
The old HF Dataset diagnostic control stays a diagnostic control. No new Task
adapter is implemented here. First evidence will be frozen only after independent
execution; later qualification must compare fresh output with exact frozen bytes.
ZIP container digests are kept separate from evidence-member digests.

## Non-effects and next boundary

No upstream code, old lock, historical results, main, Stable Core or product/runtime
files are changed. No model download/instantiation/inference, provider API, user
credentials, Hub dataset loading, scoring/judge or evaluation dispatch. No benchmark
score, novelty, production, release, standards or merge authority.

The next separate gate is a clearly named model-free Task adapter addressing
configuration and list-to-Dataset conversion, only after this environment is
qualified. Draft/open; `merge_authorized=false`. #983 is not edited or promoted.
