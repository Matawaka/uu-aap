# v0.14 evidence boundary

The stored first observation is a successful **selected environment** result, not
an upstream Task compatibility result. First head:
`a6abd8ba9f382be48774a609496cebf09d9ceeaf`; run `34222490507`.
Both predeclared A/B jobs completed successfully on distinct hosted runners; their
13 downloaded evidence members match byte-for-byte. Probe code remains
`8390dc543e838d1aaea7a3835de3f36a6a8f3650`.

129 installed distributions; 267 active requirement edges; 15580 namespace files
verified against one owner and matching RECORD sizes/hashes. Public imports match
before/after in fresh interpreters. Old v0.11/v0.12/v0.13 probe outputs retain their
exact hashes; upstream Task compatibility remains NONPASS.

The stored result, profile, installed-membership list, evidence-member manifest
and first-run qualification receipt are durable Git evidence. Full census and
probe artifacts are supplementary (30-day Actions retention). The receipt binds
their exact members, not their ZIP container identity.

A successor head is qualified only when BOTH jobs also pass the unconditional
fresh-evidence validator after executing the unchanged probe code. The workflow
never copies frozen files into the runtime output directory. Missing evidence,
changed digests, changed history or promoted Task status fail closed.

#983 remains blocked on its historical full environment. v0.14 is an explicitly
different installation selection: old lock unchanged, conflicting distribution
never installed, plugin used only as pinned external source. No old environment
or receipt is reinterpreted, and this is not a full dependency-filesystem or
hermetic-build audit.

Next separate gate: an explicitly named model-free Task adapter for configuration
and list-to-Dataset conversion. No adapter, scoring, model, main update, release,
standards or merge authority is established here.
