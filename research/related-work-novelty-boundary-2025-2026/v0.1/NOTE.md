# Qualification note

`implementation-receipt.json` is deliberately `CANDIDATE_UNQUALIFIED` until independent repository CI executes the validator and hostile suite on the pull-request head.

It must not be manually rewritten to `QUALIFIED_BOUNDED` merely because the files exist or because a pull request is opened.

`Materialized candidate != independently qualified research result`.
