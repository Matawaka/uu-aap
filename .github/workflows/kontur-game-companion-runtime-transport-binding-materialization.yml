name: KONTUR Game Companion runtime transport binding materialization validation

on:
  pull_request:
    paths:
      - 'pilots/kontur-game-companion/runtime-transport-binding-materialization/**'
      - 'pilots/kontur-game-companion/runtime-transport-binding-grant/**'
      - 'pilots/kontur-game-companion/runtime-transport-binding-review/**'
      - 'pilots/kontur-game-companion/runtime-transport-binding-challenge/**'
      - '.github/workflows/kontur-game-companion-runtime-transport-binding-materialization.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-runtime-transport-binding-materialization:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate runtime transport binding materialization
        run: python pilots/kontur-game-companion/runtime-transport-binding-materialization/validate.py
