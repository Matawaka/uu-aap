name: KONTUR Game Companion network user-surface externalization review validation

on:
  pull_request:
    paths:
      - 'pilots/kontur-game-companion/network-user-surface-externalization-review/**'
      - 'pilots/kontur-game-companion/network-user-surface-activation-challenge/**'
      - 'pilots/kontur-game-companion/runtime-transport-binding-materialization/**'
      - '.github/workflows/kontur-game-companion-network-user-surface-externalization-review.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-network-user-surface-externalization-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate network user-surface externalization review receipt
        run: python pilots/kontur-game-companion/network-user-surface-externalization-review/validate.py
